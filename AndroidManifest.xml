<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.ginkel.passwordhasher"
      android:versionCode="2"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/AppName" android:name=".GenieApplication">
        <activity android:name=".TabbedMainActivity"
                  android:label="@string/AppName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

			<!-- browser integration -->
			<intent-filter android:label="@string/AppName">
				<action android:name="android.intent.action.SEND" />
				<category android:name="android.intent.category.DEFAULT" />
				<data android:mimeType="text/plain" />
			</intent-filter>
        </activity>
        <activity android:name=".MainActivity"
                  android:label="@string/AppName">
        </activity>
    	<activity android:name=".SettingsActivity"
    	          android:label="@string/Title_Settings">
    	</activity>
	</application>
    <uses-sdk android:minSdkVersion="4" />
</manifest> 